"""
kerno CLI: run tasks, inspect sessions, manage configuration.

Commands:
  kerno run       — run a task
  kerno session   — inspect past sessions
  kerno memory    — manage the memory store
  kerno config    — show / generate configuration
  kerno doctor    — diagnose the environment
"""
